// Manually-bumped version strings. Bump BUILD_VERSION on every APK
// build, OTA_VERSION on every JS-only OTA push.
export const BUILD_VERSION = "build #1 - initial";
export const OTA_VERSION   = "OTA #1 - initial";
